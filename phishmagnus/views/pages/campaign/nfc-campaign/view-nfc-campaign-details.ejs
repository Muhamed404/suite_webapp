<div class="flex flex-col  p-6 pt-0">
    <!-- first section  -->
    <div class="flex flex-col gap-4 mt-3">
        <!-- Title -->
        <h1 class="text-2xl font-semibold my-2"><%= __('qr_campaign.phishingCampaignResponseMetrics') %></h1>

        <!-- Top Metrics -->
        <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
            <div class="bg-white p-4 flex flex-col rounded-xl">
                <div class="flex items-center justify-between gap-2 mb-2">
                    <p class="text-gray-500 text-sm"><%= __('nfc_campaign.totalTags') %></p>
                    <img src="/securemagnus_2025/images/nfc_sent.svg" alt="">
                </div>
                <p class="text-4xl">
                    <%=scannedNotScannedStats.totalDevices%>
                </p>
            </div>
            <div class="bg-white p-4 flex flex-col rounded-xl">
                <div class="flex items-center justify-between gap-2 mb-2">
                    <p class="text-gray-500 text-sm"><%= __('nfc_campaign.notScanned') %></p>
                    <img src="/securemagnus_2025/images/nfc-notsent.svg" alt="">
                </div>
                <p class="text-4xl">
                    <%=scannedNotScannedStats.notScannedCount%>
                </p>
            </div>
            <div class="bg-white p-4 flex flex-col rounded-xl">
                <div class="flex items-center justify-between gap-2 mb-2">
                    <p class="text-gray-500 text-sm"><%= __('nfc_campaign.totalScanned') %></p>
                    <img src="/securemagnus_2025/images/nfc_sent.svg" alt="">
                </div>
                <p class="text-4xl">
                    <%=campaignStats.number_of_scans%>
                </p>
            </div>
            <div class="bg-white p-4 flex flex-col rounded-xl">
                <div class="flex items-center justify-between gap-2 mb-2">
                    <p class="text-gray-500 text-sm"><%= __('nfc_campaign.opened') %></p>
                    <img src="/securemagnus_2025/images/nfc-opened.svg" alt="">
                </div>
                <p class="text-4xl">
                    <%=campaignStats.is_opened%>
                </p>
            </div>
            <div class="bg-white p-4 flex flex-col rounded-xl">
                <div class="flex items-center justify-between gap-2 mb-2">
                    <p class="text-gray-500 text-sm"><%= __('generic_label.formInteraction') %></p>
                    <img src="/securemagnus_2025/images/nfc-admin.svg" alt="">
                </div>
                <p class="text-4xl">
                    <%=campaignStats.is_interacted%>
                </p>
            </div>
            <div class="bg-white p-4 flex flex-col rounded-xl">
                <div class="flex items-center justify-between gap-2 mb-2">
                    <p class="text-gray-500 text-sm"><%= __('generic_label.formSubmitted') %></p>
                    <img src="/securemagnus_2025/images/nfc-people.svg" alt="">
                </div>
                <p class="text-4xl">
                    <%=campaignStats.is_submitted%>
                </p>
            </div>
            <div class="bg-white p-4 flex flex-col rounded-xl">
                <div class="flex items-center justify-between gap-2 mb-2">
                    <p class="text-gray-500 text-sm"><%= __('nfc_campaign.fileDownloaded') %></p>
                    <img src="/securemagnus_2025/images/nfc-download.svg" alt="">
                </div>
                <p class="text-4xl">
                    <%=campaignStats.is_downloaded%>
                </p>
            </div>
        </div>

        <!-- Main Section -->
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-4">
            <!-- Bar Chart -->
            <div class="bg-white p-6 rounded-xl   col-span-2">
                <h2 class="text-lg font-semibold mb-4"><%= __('nfc_campaign.campaignMetrics') %></h2>
                <div id="barChart"></div>
            </div>

            <!-- Phishing Success -->
            <div class="bg-white p-6 rounded-xl gap-11  flex flex-col items-start  justify-start">
                <h2 class="text-lg font-semibold mb-4"><%= __('nfc_campaign.phishingSuccess') %></h2>

                <div class="flex items-center  gap-10 w-full justify-between">
                    <!-- Left Side Labels -->
                    <div class="space-y-6">
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-[#3b82f6]"></span> <!-- Blue -->
                            <div>
                                <p class="text-gray-500 text-sm"><%= __('nfc_campaign.nfcScan') %></p>
                                <p class="text-lg font-bold text-gray-900" id="scanText"><%= __('generic_label.people') %></p>
                            </div>
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-[#9ca3af]"></span> <!-- Gray -->
                            <div>
                                <p class="text-gray-500 text-sm"><%= __('nfc_campaign.notScanned') %></p>
                                <p class="text-lg font-bold text-gray-900" id="notScanText"><%= __('generic_label.people') %></p>
                            </div>

                        </div>
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-[#fb923c]"></span> <!-- Orange -->
                            <div>
                                <p class="text-gray-500 text-sm"><%= __('nfc_campaign.interaction') %></p>
                                <p class="text-lg font-bold text-gray-900" id="formInteractionText"><%= __('generic_label.people') %></p>
                            </div>
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-[#34d399]"></span> <!-- Green -->
                            <div>
                                <p class="text-gray-500 text-sm"><%= __('nfc_campaign.download') %></p>
                                <p class="text-lg font-bold text-gray-900" id="attachmentDownloadedText"><%= __('generic_label.people') %></p>
                            </div>
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-[#ef4444]"></span> <!-- Red -->
                            <div>
                                <p class="text-gray-500 text-sm"><%= __('nfc_campaign.compromised') %></p>

                                <p class="text-lg font-bold text-gray-900" id="targetCompromisedText"><%= __('generic_label.people') %></p>
                            </div>
                        </div>
                    </div>

                    <!-- Donut Chart -->
                    <div id="successChart" style="width:50%; height:50%;"></div>
                </div>
            </div>
        </div>

        <!-- Second Row -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <!-- Employees Phishing Segments -->
            <div class="bg-white p-6 rounded-xl  ">
                <h2 class="text-lg font-semibold mb-4"><%= __('nfc_campaign.phishingSegments') %></h2>
                <div id="segmentsChart"></div>
            </div>


            <!-- Campaign Details -->
            <div class="bg-white p-6 rounded-xl  ">
                <h2 class="text-lg font-semibold text-gray-800 mb-6"><%= __('nfc_campaign.campaignDetails') %></h2>
                <div class="space-y-4">
                    
                    <div class="flex justify-between text-sm">
                        <span class="text-gray-500"><%= __('nfc_campaign.templateName') %></span>
                        <span class="text-gray-800 font-medium">
                            <%=templateDetails.name%>
                        </span>
                    </div>
                    <% if (nfcDevices && nfcDevices.length > 0 && nfcDevices[0].download_url) { %>
                    <div class="flex justify-between items-center text-sm gap-2">
                        <span class="text-gray-500 flex-shrink-0"><%= __('nfc_campaign.phishingPage') %></span>
                        <button
                            class="copyUrlButton flex items-center gap-1 text-teal-500 hover:text-white hover:bg-teal-500 border border-teal-500 px-2 py-1 rounded-lg transition text-xs"
                            data-url="<%= nfcDevices[0].download_url.replace(/([?&]ifc=)[^&]+/, '$1test') %>" title="Copy URL">
                            <%= __('nfc_campaign.copyUrl') %>
                            <img src="/securemagnus_2025/images/copy.svg" class="svg w-3.5 h-3.5" alt="">
                        </button>
                    </div>
                    <% } %>
                    <div class="flex justify-between text-sm">
                        <span class="text-gray-500"><%= __('nfc_campaign.launchDate') %></span>
                        <span class="text-gray-800 font-medium">
                            <%=campaignDetails.start_datetime%>
                        </span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Bottom Charts -->
        <div class="grid grid-cols-1 md:grid-cols-4 gap-4">

            <!-- Links Interaction -->



            <div class="bg-white p-6 rounded-xl   text-center">

                <div class="flex items-center justify-start mb-9 gap-2">
                    <img src="/securemagnus_2025/images/Links-Interaction.svg" alt="">
                    <label for="Scan Interaction"><%= __('nfc_campaign.linksInteraction') %></label>
                </div>

                <div>
                    <div class="nfcSemiChartLink" uniqueScan="<%=openSegmentStats?.uniqueScanCount%>"
                        totalScan="<%=campaignStats?.number_of_scans%>" noScan="<%=openSegmentStats?.noScanCount%>">
                    </div>
                </div>

            </div>



            <div class="bg-white p-6 rounded-xl   text-center">

                <div class="flex items-center justify-start mb-9 gap-2">
                    <img src="/securemagnus_2025/images/interactfrom.svg" alt="">
                    <label for="Interact Form "><%= __('nfc_campaign.interactForm') %></label>
                </div>

                <div>
                    <div class="nfcSemiChartInteract"
                        uniqueScan="<%=formInteractionSegmentStats?.uniqueInteractedCount%>"
                        totalInteractions="<%=campaignStats?.is_interacted%>"
                        noScan="<%=formInteractionSegmentStats?.noInteractedCount%>">
                    </div>
                </div>

            </div>


            <!-- Total Scans -->
            <div class="bg-white p-6 rounded-xl   text-center">

                <div class="flex items-center justify-start mb-9 gap-2">
                    <img src="/securemagnus_2025/images/formsubmitted.svg" alt="">
                    <label for="Form Submitted"><%= __('nfc_campaign.formSubmitted') %></label>
                </div>

                <div>
                    <div class="nfcSemiChartSubmitted" uniqueScan="<%=formSubmittedSegmentStats?.uniqueSubmittedCount%>"
                        totalSubmitted="<%=campaignStats?.is_submitted%>"
                        noScan="<%=formSubmittedSegmentStats?.noSubmittedCount%>">
                    </div>
                </div>
            </div>

            <!-- Form Submitted -->
            <div class="bg-white p-6 rounded-xl   text-center">

                <div class="flex items-center justify-start mb-9 gap-2">
                    <img src="/securemagnus_2025/images/attachmentopened.svg" alt="">
                    <label for="Attachment Opened"><%= __('nfc_campaign.attachmentOpened') %></label>
                </div>

                <div>
                    <div class="nfcSemiChartDownload" uniqueScan="<%=downloadSegmentStats?.uniqueDownloadCount%>"
                        repeatedScan="<%=downloadSegmentStats?.repeatedDownloadCount%>"
                        noScan="<%=downloadSegmentStats?.noDownloadCount%>">
                    </div>
                </div>
            </div>



        </div>


        <!-- last table  -->
        <!-- last table  -->

        <div class="flex flex-col rounded-3xl bg-white border border-gray-100 shadow-sm mt-3">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 px-6 pt-6 pb-4 border-b border-gray-100">
                <h2 class="text-2xl font-semibold text-gray-900"><%= __('nfc_campaign.interactionReport') %></h2>
                <div class="relative hidden lg:block">
                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z"/>
                        </svg>
                    </span>
                    <input id="interactionSearch" type="text" placeholder="<%= __('nfc_campaign.searchHere') %>" value="<%= typeof search !== 'undefined' ? search : '' %>"
                        class="pl-9 pr-4 py-2 rounded-full bg-gray-50 text-sm border border-gray-200 focus:outline-none focus:ring-2 focus:ring-teal-400 w-56">
                </div>
            </div>
            <!-- Table -->
            <div class="overflow-x-auto">
                <table class="min-w-full text-sm text-left">
                    <thead class="bg-gray-50 text-gray-900 text-xs uppercase tracking-wider">
                        <tr>
                            <th class="px-6 py-3 font-medium whitespace-nowrap">#</th>
                            <th class="px-6 py-3 font-medium whitespace-nowrap">
                                <div class="flex items-center gap-2">
                                    <img src="/securemagnus_2025/images/nfc_sent.svg" class="w-5 h-5" alt="">
                                    <%= __('nfc_campaign.nfcCode') %>
                                </div>
                            </th>
                            <th class="px-6 py-3 font-medium whitespace-nowrap">
                                <div class="flex items-center gap-2">
                                    <img src="/securemagnus_2025/images/nfc-opened.svg" class="w-5 h-5" alt="">
                                    <%= __('nfc_campaign.scanned') %>
                                </div>
                            </th>
                            <th class="px-6 py-3 font-medium whitespace-nowrap">
                                <div class="flex items-center gap-2">
                                    <img src="/securemagnus_2025/images/copy.svg" class="w-5 h-5" alt="">
                                    <%= __('nfc_campaign.copyUrl') %>
                                </div>
                            </th>
                            <th class="px-6 py-3 font-medium whitespace-nowrap">
                                <div class="flex items-center gap-2">
                                    <img src="/securemagnus_2025/images/eye.svg" class="w-5 h-5" alt="">
                                    <%= __('nfc_campaign.viewReport') %>
                                </div>
                            </th>
                        </tr>
                    </thead>
                    <tbody id="interactionTableBody" class="divide-y divide-gray-100">
                        <% if (typeof nfcDevices !== 'undefined' && nfcDevices !== null && nfcDevices.length > 0) {
                            nfcDevices.forEach((device, index) => { %>
                            <tr class="bg-white hover:bg-gray-50 transition-colors duration-150">
                                <td class="px-6 py-4 text-gray-600 font-medium whitespace-nowrap"><%=index + 1%></td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold bg-blue-50 text-blue-700">
                                        <%=device.nfc_code %>
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <% if (device.is_scanned) { %>
                                        <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold bg-teal-100 text-teal-700">
                                            <span class="w-1.5 h-1.5 rounded-full bg-teal-500 inline-block"></span>
                                            <%= __('nfc_campaign.scannedYes') %>
                                        </span>
                                    <% } else { %>
                                        <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold bg-gray-100 text-gray-500">
                                            <span class="w-1.5 h-1.5 rounded-full bg-gray-400 inline-block"></span>
                                            <%= __('nfc_campaign.scannedNo') %>
                                        </span>
                                    <% } %>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <button class="copyUrlButton inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium text-teal-600 border border-teal-200 bg-teal-50 hover:bg-teal-600 hover:text-white transition-colors duration-150"
                                        data-url="<%=device.download_url%>" title="Copy URL">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"/>
                                        </svg>
                                        <%= __('nfc_campaign.copyUrl') %>
                                    </button>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <a href="/phm/campaign/nfc/device/report/<%= campaignDetails.id %>/<%=device.nfc_code %>"
                                        class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium text-blue-600 border border-blue-200 bg-blue-50 hover:bg-blue-600 hover:text-white transition-colors duration-150">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                                        </svg>
                                        <%= __('nfc_campaign.viewReport') %>
                                    </a>
                                </td>
                            </tr>
                        <% }); } else { %>
                            <tr>
                                <td colspan="5" class="px-6 py-10 text-center text-gray-400 text-sm">
                                    <svg class="w-8 h-8 mx-auto mb-2 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 17v-6a2 2 0 012-2h2a2 2 0 012 2v6m-6 0h6"/>
                                    </svg>
                                    <%= __('nfc_campaign.noRecords') %>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
            <div id="interactionPagination" class="px-5 pb-5"></div>
        </div>
    </div>
</div>



<script>
    $(document).on('click', '.copyUrlButton', function () {
        // alert('Copy URL button clicked');
        const url = $(this).data('url');
        navigator.clipboard.writeText(url)
            .then(() => showCustomToast('success', 'URL copied!'))
            .catch(() => showCustomToast('error', 'Failed to copy URL'));
    });

    window.localizedLabels = {
        people: '<%= __("generic_label.people") %>',
        nfcScan: '<%= __("nfc_campaign.nfcScan") %>',
        notScanned: '<%= __("nfc_campaign.notScanned") %>',
        interaction: '<%= __("nfc_campaign.interaction") %>',
        download: '<%= __("nfc_campaign.download") %>',
        compromised: '<%= __("nfc_campaign.compromised") %>',
        devicesSent: '<%= __("nfc_campaign.totalTags") %>',
        totalScans: '<%= __("nfc_campaign.totalScanned") %>',
        openedLink: '<%= __("nfc_campaign.opened") %>',
        interactForm: '<%= __("generic_label.formInteraction") %>',
        submitForm: '<%= __("generic_label.formSubmitted") %>',
        downloaded: '<%= __("nfc_campaign.fileDownloaded") %>',
        totalDevices: '<%= __("nfc_campaign.totalTags") %>',
        totalNotScanned: '<%= __("nfc_campaign.notScanned") %>',
        totalScanned: '<%= __("nfc_campaign.totalScanned") %>',
        totalInteractions: '<%= __("nfc_campaign.interaction") %>',
        totalAttachmentDownloaded: '<%= __("nfc_campaign.fileDownloaded") %>',
        openNotReported: '<%= __("nfc_campaign.openNotReported") %>',
        openedReported: '<%= __("nfc_campaign.openedReported") %>',
        notOpenedReported: '<%= __("nfc_campaign.notOpenedReported") %>',
        uniqueScans: '<%= __("nfc_campaign.uniqueScans") %>',
        repeatedScans: '<%= __("nfc_campaign.repeatedScans") %>',
        noScans: '<%= __("nfc_campaign.noScans") %>',
        repeatedInteractions: '<%= __("nfc_campaign.repeatedInteractions") %>',
        repeatedSubmitted: '<%= __("nfc_campaign.repeatedSubmitted") %>',
        repeatedDownloads: '<%= __("nfc_campaign.repeatedDownloads") %>',
        uniqueInteractions: '<%= __("nfc_campaign.uniqueInteractions") %>',
        noInteractions: '<%= __("nfc_campaign.noInteractions") %>',
        uniqueSubmitted: '<%= __("nfc_campaign.uniqueSubmitted") %>',
        noSubmitted: '<%= __("nfc_campaign.noSubmitted") %>',
        scannedYes: '<%= __("nfc_campaign.scannedYes") %>',
        scannedNo: '<%= __("nfc_campaign.scannedNo") %>',
        copyUrl: '<%= __("nfc_campaign.copyUrl") %>',
        viewReport: '<%= __("nfc_campaign.viewReport") %>',
        noRecords: '<%= __("nfc_campaign.noRecords") %>'
    };

    window.CampaignMetrics = {
        openedLink: <%= typeof campaignStats !== 'undefined' && campaignStats?.is_opened || 0 %>,
        interactForm: <%= typeof campaignStats !== 'undefined' && campaignStats?.is_interacted || 0 %>,
        submitForm: <%= typeof campaignStats !== 'undefined' && campaignStats?.is_submitted || 0 %>,
        attachmentDownloaded: <%= typeof campaignStats !== 'undefined' && campaignStats?.is_downloaded || 0 %>,
        totalScans: <%= typeof campaignStats !== 'undefined' && campaignStats?.number_of_scans || 0 %>,
        totalNotScans: <%= typeof scannedNotScannedStats !== 'undefined' && scannedNotScannedStats?.notScannedCount || 0 %>,
        totalNFCDevices: <%= typeof scannedNotScannedStats !== 'undefined' && scannedNotScannedStats?.totalDevices || 0 %>,
    };

    window.nfcDevices = <%- JSON.stringify(nfcDevices || []) %>;

    // Search functionality (calls backend with debounce as user types)
    let interactionSearchTimer = null;
    const interactionSearchDelay = 350;
    const interactionPagination = document.getElementById('interactionPagination');
    const interactionPageState = {
        page: 1,
        pageSize: 10,
        totalPages: 1,
        totalCount: 0,
        devices: []
    };

    function updateInteractionPagination(devices, page) {
        interactionPageState.devices = Array.isArray(devices) ? devices : [];
        interactionPageState.totalCount = interactionPageState.devices.length;
        interactionPageState.totalPages = interactionPageState.totalCount === 0
            ? 0
            : Math.max(1, Math.ceil(interactionPageState.totalCount / interactionPageState.pageSize));
        interactionPageState.page = Math.min(Math.max(1, page || 1), interactionPageState.totalPages || 1);
    }

    function renderInteractionPagination() {
        if (!interactionPagination) {
            return;
        }

        const currentPageNum = interactionPageState.page;
        const totalPages = interactionPageState.totalPages;
        const pageSize = interactionPageState.pageSize;
        const totalCount = interactionPageState.totalCount;

        if (totalPages < 1) {
            interactionPagination.innerHTML = '';
            return;
        }

        const startResult = totalCount === 0 ? 0 : ((currentPageNum - 1) * pageSize) + 1;
        const endResult = totalCount === 0 ? 0 : Math.min(currentPageNum * pageSize, totalCount);

        let paginationHTML = `
            <div class="flex items-center justify-between w-full">
                <div class="${document.dir === "rtl" ? "ml-4 mr-2" : "mr-4"}">
                    <p class="text-sm text-gray-700">
                        Showing ${startResult}
                        to ${endResult}
                        of ${totalCount} results
                    </p>
                </div>
                <div class="flex space-x-2">
        `;

        if (currentPageNum > 1) {
            paginationHTML += `
                <button type="button" data-page="${currentPageNum - 1}"
                    class="px-3 py-1 rounded-full border text-gray-600 hover:bg-teal-50 hover:text-[var(--teal)] hover:border hover:border-[var(--teal)] duration-300">
                    ‹
                </button>
            `;
        }

        let startPage = Math.max(1, currentPageNum - 1);
        let endPage = Math.min(totalPages, currentPageNum + 1);

        if (endPage - startPage < 2) {
            if (startPage === 1) {
                endPage = Math.min(totalPages, 3);
            } else if (endPage === totalPages) {
                startPage = Math.max(1, totalPages - 2);
            }
        }

        for (let i = startPage; i <= endPage; i += 1) {
            const isActive = i === currentPageNum;
            paginationHTML += `
                <button type="button" data-page="${i}"
                    class="px-3 py-1 rounded-full ${isActive ? "bg-teal-50 text-[var(--teal)] border border-[var(--teal)]" : "border text-gray-600 hover:bg-teal-50 hover:text-[var(--teal)] hover:border hover:border-[var(--teal)] duration-300"}">
                    ${i}
                </button>
            `;
        }

        if (currentPageNum < totalPages) {
            paginationHTML += `
                <button type="button" data-page="${currentPageNum + 1}"
                    class="px-3 py-1 rounded-full border text-gray-600 hover:bg-teal-50 hover:text-[var(--teal)] hover:border hover:border-[var(--teal)] duration-300">
                    ›
                </button>
            `;
        }

        paginationHTML += `
                </div>
            </div>
        `;

        interactionPagination.innerHTML = paginationHTML;

        interactionPagination.querySelectorAll('button[data-page]').forEach(button => {
            button.addEventListener('click', () => {
                const nextPage = Number(button.dataset.page || 1);
                renderInteractionPage(interactionPageState.devices, nextPage);
            });
        });
    }

    function renderInteractionPage(devices, page) {
        updateInteractionPagination(devices, page);
        const startIndex = (interactionPageState.page - 1) * interactionPageState.pageSize;
        const pageItems = interactionPageState.devices.slice(
            startIndex,
            startIndex + interactionPageState.pageSize
        );
        renderInteractionTable(pageItems);
        renderInteractionPagination();
    }

    function fetchInteractionSearchResults(rawSearch) {
        const search = (rawSearch || '').trim();
        const url = new URL(window.location.href);
        url.searchParams.set('ajax', '1');

        if (search) {
            url.searchParams.set('search', search);
        } else {
            url.searchParams.delete('search');
        }

        $.ajax({
            url: url.toString(),
            method: 'GET',
            success: function (response) {
                renderInteractionPage(response.nfcDevices, 1);
                const stateUrl = new URL(window.location.href);
                if (search) {
                    stateUrl.searchParams.set('search', search);
                } else {
                    stateUrl.searchParams.delete('search');
                }
                history.replaceState({}, '', stateUrl.toString());
            },
            error: function (error) {
                console.error('Error fetching search results:', error);
                showCustomToast('error', 'Failed to fetch search results');
            }
        });
    }

    $('#interactionSearch').on('input', function () {
        const search = $(this).val();
        clearTimeout(interactionSearchTimer);
        interactionSearchTimer = setTimeout(function () {
            fetchInteractionSearchResults(search);
        }, interactionSearchDelay);
    });

    $('#interactionSearch').on('keydown', function (e) {
        if (e.key === 'Enter') {
            e.preventDefault();
            clearTimeout(interactionSearchTimer);
            fetchInteractionSearchResults($(this).val());
        }
    });

    $(function () {
        const initialSearch = $('#interactionSearch').val();
        if (initialSearch) {
            fetchInteractionSearchResults(initialSearch);
        } else {
            renderInteractionPage(window.nfcDevices || [], 1);
        }
    });

    function renderInteractionTable(devices) {
        const $tbody = $('#interactionTableBody');
        $tbody.empty();

        if (devices && devices.length > 0) {
            devices.forEach((device, index) => {
                const row = `
                    <tr class="bg-white hover:bg-gray-50 transition-colors duration-150">
                        <td class="px-6 py-4 text-gray-600 font-medium whitespace-nowrap">${index + 1}</td>
                        <td class="px-6 py-4 whitespace-nowrap">
                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-semibold bg-blue-50 text-blue-700">
                                ${device.nfc_code}
                            </span>
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap">
                            ${device.is_scanned ? `
                                <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold bg-teal-100 text-teal-700">
                                    <span class="w-1.5 h-1.5 rounded-full bg-teal-500 inline-block"></span>
                                    ${window.localizedLabels.scannedYes}
                                </span>
                            ` : `
                                <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold bg-gray-100 text-gray-500">
                                    <span class="w-1.5 h-1.5 rounded-full bg-gray-400 inline-block"></span>
                                    ${window.localizedLabels.scannedNo}
                                </span>
                            `}
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap">
                            <button class="copyUrlButton inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium text-teal-600 border border-teal-200 bg-teal-50 hover:bg-teal-600 hover:text-white transition-colors duration-150"
                                data-url="${device.download_url}" title="Copy URL">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"/>
                                </svg>
                                ${window.localizedLabels.copyUrl}
                            </button>
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap">
                            <a href="/phm/campaign/nfc/device/report/<%= typeof campaignDetails !== 'undefined' ? campaignDetails.id : '' %>/${device.nfc_code}"
                                class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-medium text-blue-600 border border-blue-200 bg-blue-50 hover:bg-blue-600 hover:text-white transition-colors duration-150">
                                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                                </svg>
                                ${window.localizedLabels.viewReport}
                            </a>
                        </td>
                    </tr>
                `;
                $tbody.append(row);
            });
        } else {
            $tbody.append(`
                <tr>
                    <td colspan="5" class="px-6 py-10 text-center text-gray-400 text-sm">
                        <svg class="w-8 h-8 mx-auto mb-2 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 17v-6a2 2 0 012-2h2a2 2 0 012 2v6m-6 0h6"/>
                        </svg>
                        ${window.localizedLabels.noRecords}
                    </td>
                </tr>
            `);
        }
    }


</script>
<script src="/securemagnus_2025/js/nfc_camp_view.js"></script>
<script src="/securemagnus_2025/js/icons.js"></script>
